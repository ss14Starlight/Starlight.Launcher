@using Starlight.Launcher.WebUI.Localization;

@inherits LocalizedComponentBase

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            @L.GetString("changelog-title", ("version", Version))
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudPaper Class="pa-2" Style="max-height:60vh; overflow-y:auto;" Elevation="0">
            @if (string.IsNullOrWhiteSpace(Notes))
            {
                <MudText>@L["changelog-empty"]</MudText>
            }
            else
            {
                <MudMarkdown Value="@Notes" />
            }
        </MudPaper>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="Close">
            @L["changelog-close"]
        </MudButton>
    </DialogActions>
</MudDialog>
