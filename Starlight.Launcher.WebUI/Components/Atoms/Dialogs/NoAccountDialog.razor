@using Starlight.Launcher.WebUI.Localization;

@inherits LocalizedComponentBase

<MudDialog>
    <TitleContent>
        <div class="d-flex align-center gap-3">
            <div class="sl-dialog-icon">
                <MudIcon Icon="@Icons.Material.Rounded.AccountCircle" Size="Size.Small" />
            </div>
            <MudText Typo="Typo.h6">
                @L["no-account-dialog-title"]
            </MudText>
        </div>
    </TitleContent>
    <DialogContent>
        <MudPaper Elevation="0"
                  Class="pa-4 d-flex align-center gap-3"
                  Style="background-color: var(--mud-palette-background-gray); border-radius: 12px;">
            <MudIcon Icon="@Icons.Material.Outlined.Info"
                     Color="Color.Info"
                     Size="Size.Medium" />
            <MudText Typo="Typo.body1">
                @L["no-account-dialog-description"]
            </MudText>
        </MudPaper>
    </DialogContent>
    <DialogActions>
        <div class="d-flex gap-2 pa-2">
            <MudButton OnClick="Cancel"
                       Variant="Variant.Text"
                       Color="Color.Default">
                @L["general-cancel"]
            </MudButton>
            <MudButton OnClick="Success"
                       Variant="Variant.Filled"
                       Class="cta-btn-primary"
                       EndIcon="@Icons.Material.Rounded.ArrowForward">
                @L["general-ok"]
            </MudButton>
        </div>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] private IMudDialogInstance MudDialog { get; set; } = default!;
    [Inject] private IDialogService _dialogService { get; set; } = default!;
    [Parameter] public string Address { get; set; } = "";
    [Parameter] public string? Title { get; set; } = null;

    private async Task Success()
    {
        MudDialog.Close();
        var parameters = new DialogParameters<ConnectingDialog>
        {
            { x => x.Address, Address },
            { x => x.Title, Title }
        };
        var options = new DialogOptions
        {
            BackdropClick = false,
            CloseOnEscapeKey = false,
            CloseButton = false,
            MaxWidth = MaxWidth.ExtraSmall,
            FullWidth = true
        };
        await _dialogService.ShowAsync<ConnectingDialog>("Connecting", parameters, options);
    }

    private void Cancel() => MudDialog.Cancel();
}
